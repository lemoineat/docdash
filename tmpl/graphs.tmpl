<?js
    var data = obj;
    var self = this;

    data.forEach(function(graph) {
        if (graph.name) {
    ?>
        <p class="code-caption"><?js= graph.name ?></p>
        <img src="images/<?js= graph.name ?>.png" />
    <?js } 
    });
?>